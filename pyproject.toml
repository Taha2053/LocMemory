[project]
name = "locmemory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.6",
    "ollama>=0.6.1",
    "pyyaml>=6.0.3",
    "sentence-transformers>=5.2.3",
    "pytest>=7.0.0",
]
